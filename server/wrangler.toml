name = "blackmirror-api"
main = "src/index.js"
compatibility_date = "2024-03-20"

# Note: Secrets like TURSO_DATABASE_URL and TURSO_AUTH_TOKEN
# should be added via `npx wrangler secret put` for production,
# and placed in `.dev.vars` for local development.
